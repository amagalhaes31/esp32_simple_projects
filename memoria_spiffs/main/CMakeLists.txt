# Edit following two lines to set component requirements (see docs)
set(COMPONENT_SRCS "main.c")
set(COMPONENT_ADD_INCLUDEDIRS ".")
register_component()
spiffs_create_partition_image(arquivos ../arquivos_spiffs FLASH_IN_PROJECT)
